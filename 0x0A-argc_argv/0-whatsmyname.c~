#include <stdlib.h>
#include <stdio.h>

/**
 * main - function to print the name of the function
 * @argc: number of arguments passed into program
 * @aargv: vector of the arguments i.e contents of the string
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
