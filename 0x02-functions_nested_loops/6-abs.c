#include "main.h"
/**
 * _abs - function to get absolute
 * @int: any integer passed into the function
 * Return: 0
 */

int _abs(int)
{
	int c;

	if (c >= 0)
		return (c);
	else if (c < 0)
		return (c * -1);

}
