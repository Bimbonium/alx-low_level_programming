
#include <stdio.h>
#include <stdlib.h>
/**
 * main - function to print alphabets in upper and lower case
 * Return:0
 *Betty is really beginning to get on my nerves
 *Again this code would have been better using just a straight
 *character declaration, even though it will not be dynamic
 */

int main(void)
{

	char a;
	char b;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	for (b = 'A'; A <= 'Z'; A++)
		putchar(b);
	putchar('\n');
	return (0);
}
